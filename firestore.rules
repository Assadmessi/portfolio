rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for portfolio content; only authenticated users can write.
    match /portfolio/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
